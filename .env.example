# Copy this file to .env (or set these as real environment variables on your host)
# and NEVER commit real secrets to version control.

CROPGUARD_SECRET_KEY=change-this-to-a-long-random-string
CROPGUARD_ADMIN_USER=host
CROPGUARD_ADMIN_PASS=change-this-strong-password
FLASK_DEBUG=0
PORT=5000
